apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: monitoring
spec:
  selector:
    app: monitoring-backend
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
